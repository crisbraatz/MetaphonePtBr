using System.Text;

namespace MetaphonePtBr.Letters
{
    internal static class Y
    {
        /// Legend:
        /// Letter = Letter.
        /// Rules ordered by priority:
        /// Y = I.
        internal static void Convert(StringBuilder token) => token.Append('I');
    }
}
